# GT-lineage resolution addendum — 2026-08-30

## Current modern-screen completion addendum — 2026-08-30

The inventory now has a separate completion receipt for the modern screen.
`gemini-3.5-flash` and `gemini-3.5-flash-lite` each have 5,598 terminal rows
over the 622-document, nine-view matrix. The modern analysis used 3,718 raw
six-name nonblank cells and did not apply the historical 3,684-row exclusion;
this is explicitly recorded in the derived-table denominator metadata. The
3.7 route is HTTP 403 and Qwen is HTTP 500 endpoint-not-found, so those model
IDs have no full PA screen. See `MODERN_FULL_RECEIPT.md`.

Statements below that say modern execution was pending belong to the earlier
inventory snapshot and are retained for audit history.

The earlier “canonical 3,684 filter remains unresolved” wording is superseded.
The exact six metric fields are recovered from the collapse script and v9
analysis config. The historical `_add_flags` implementation in
`chat2rec/processing/common.py` scans the wider configured GT field universe
for `stillborn`, `infant`, `know`, `maiden`, or `baby`, then removes `Self*`
rows on flagged records. It flags 24 records in the newer paper-lineage GT,
giving `622 × 6 − 24 × 2 = 3,684` row slots. The v9/v10 outputs retain one
blank row despite the configured `f_gt_missing: 0`; see
[`docs/GT_LINEAGE.md`](../docs/GT_LINEAGE.md) for the exact source hashes and
the strict-nonblank distinction.

The older `f1b978...` processed GT is the public-release/v7 lineage, yielding
3,682 rows. It is not interchangeable with the newer `a5f0...` paper-lineage
GT. Existing inventory statements below are retained for audit history.

---

# Targeted evidence addendum — 2026-08-29

This addendum refines the original bounded inventory below. No provider/network/inference call occurred.

- WARP config SHA-256: `bb5b5a8fe53381f3139a413d148d8aa5bd74ebde51a4854d91679007ed95164c`; 14 active variants and 18 commented variants. The 10-row ranking TSV contains `shift_only` plus nine mask-aware warp rows; active `warp_hw_d003_r30_s10_std{2,15,3}` has no TSV row.
- Exact v4 top three and all parameter dictionaries/ranks are now in `local_agent/SHORTLIST_EVIDENCE.md`. `std15` means 1.5. Registry inspection proves `handwriting_kernel_warp` wiring; `gridwarp.py`/`gridwarp2.py` are not the active WARP YAML implementation.
- SHIFT `run_settings.csv` has three rows: diagonal 20 variants, horizontal 33, vertical 32, all parsed CSV-safely. The adjacent weighted-CER rows are `0.09329832717612387`, `0.09350656538403`, and `0.0931695495848084`; these are sweep-level, not per-variant scores.
- Candidate historical `5164_gts.csv` files split into two non-interchangeable schema/hash groups: 20-column `14219f17...414ccd` and 18-column `cf24e896...5810d`, all 622 rows. The paper/v9/v10 processed GT and the older public/v7 processed GT are separate 30-column lineages; see the 2026-08-30 addendum above.
- Exact modern IDs are now locally evidenced: `gemini-3.7-flash`, `gemini-3.5-flash-lite`, and `sagemaker-qwen3-vl-8b-instruct-fp8`. Prior direct raw metadata also proves `gemini-3.5-flash` and `gemini-3.1-flash-lite`.
- Route details and blockers are frozen in `local_agent/ROUTE_AUDIT.md`. Existing exact-ID Gemini batch routes are proven but complex; the simpler direct Gemini route needs requested-ID smoke verification. Qwen uses 1,280-pixel longest-side RGB JPEG quality 85 inline transport, thinking off, 180-second scored timeout, one retry, and capacity/five-error stop; its active-traffic keepalive must be disabled to fit the ledger.
- Source hashes and repository SHAs used in this pass are recorded in the new artifacts. External worktrees were already dirty and were read only.

---

---

> **Everything below this line is the original bounded-inventory pass.**
> It is retained as an audit trail. Where a statement is contradicted by
> the addendum above, it is marked **SUPERSEDED** inline rather than
> deleted, per `AGENTS.md`'s source-of-truth and audit-trail rules.
> Statements not marked superseded are still active/unresolved.

# Historical inventory (bounded roots)

Generated by a bounded, depth-limited, time-boxed inventory pass. Every
listing below used `find`/`rtk find` with an explicit `-maxdepth` and a
`timeout` wrapper (15-20s per command); no unbounded recursive scan of
`the user profile root`, the user home, or OneDrive was performed or resumed.

## 1. `repository pa-death-records-622`

Public-release-style repository. `LICENSE.md` states: documentation/helper
files are for public release reference; the image scans and label data in the
*downloadable release archive* are for research use and may remain subject to
underlying source-record access terms; the repo does not grant rights beyond
what source-record holders permit.

Contents (depth 3, 11 files):
- `CITATION.cff`, `DATA_DICTIONARY.md`, `LICENSE.md`, `README.md`,
  `RELEASE_NOTES.md`
- `examples/README.md`, `examples/example_labels.csv`
- `examples/samples/README.md` + 2 sample JPGs
  (`41381_1220705043_0549-04785.jpg`, `41381_1220705043_0567-00432.jpg`) —
  these two image basenames also recur inside the `chat2rec_analysis` gemini
  response tree, confirming shared cohort identity across repos.
- `scripts/verify_release.py`

## 2. `PA_DEATH historical root`

Historical run tree. Top-level dirs (13, depth 1): `BIG_SHIFT`, `CONSISTENCY`,
`CVPR_ANALYSIS`, `LINE_THICKNESS`, `MONOTONIC_DEGRADATION`,
`MONOTONIC_DEGRADATION2`, `PROMPT_VARIATION`, `PROMPT_VARIATION_GPT`, `ROTATE`,
`SHIFT`, `WARP`, `prompts`, `temp_downloads`.

Common pattern across experiment-family dirs: `5164_gts.csv` /
`5164_gts.pkl` (+ `_no_post_processing` variants) — **SUPERSEDED in part**:
this pass described these as "one canonical ground-truth set reused per
family." The addendum above corrects this: the recurring basename actually
resolves to two non-interchangeable schema/hash groups (20-column
`14219f17...414ccd` vs 18-column `cf24e896...5810d`), so "canonical" here
means consistent *basename* reuse, not a single-file/single-schema label
set. The `metrics_no_punc/` subdirectory description below is still
active/unresolved:

`metrics_no_punc/` subdirectory with
`experiment_level_consensus.{csv,pkl}`, `master_sample_database*.{pkl,tsv}`,
`weighted_cer_by_experiment.tsv`, `run_settings.csv`.

Notable specifics:
- `WARP/`: `PA_DEATH_WARP.yaml`, `PA_DEATH_WARP_GALOIS.yaml`,
  `PA_DEATH_WARP_LINE_THICKNESS.yaml`,
  `PA_DEATH_WARP_LINE_THICKNESS_GALOIS.yaml`, `PA_DEATH_WARP_NO_SHIFT.yaml`.
  `metrics_no_punc/ensemble_selection_analysis.tsv` is the parameter-selection
  evidence (see `GOALS_AUDIT.md`). Model: `models/gemini-2.0-flash`,
  `temperature: 0`, prompt `prompts/prompt_v1.49_confidence.txt`.
  14 active (uncommented) experiment variants; a second `r60`/`d005` block is
  fully commented out (not run).
- `SHIFT/`: raw response dirs directly at depth 1, e.g.
  `gemini-20-flash_ns1_prompt12_confidence_resize-5/` (1,248 JSON responses +
  2 txt). `metrics_no_punc/run_settings.csv` exists; experiment-name column
  contains at least `PA_DEATH_FFT_SHIFT_DIAGONAL` and
  `PA_DEATH_FFT_SHIFT_HORIZONTAL` (header wraps a long prompt-text field —
  needs CSV-aware parsing, a raw grep is not reliable here). **SUPERSEDED /
  RESOLVED**: the addendum above confirms this file has exactly three rows
  (diagonal 20 variants, horizontal 33, vertical 32), parsed CSV-safely,
  with weighted-CER values `0.09329832717612387`, `0.09350656538403`, and
  `0.0931695495848084` — these are sweep-level, not per-variant, scores. The
  "needs CSV-aware extraction" follow-up flagged here is complete.
- `BIG_SHIFT/`: `metrics_no_punc/` includes `BIG_FFT.zip`, `FFT.zip`,
  `name_fields_category_summary.csv`, `name_fields_consensus_by_category.csv`.
- `CVPR_ANALYSIS/`: presentation-relevant primary source.
  `big_shift_horizontal_fft_{freq,peaks,period}.{png,csv,tex}`,
  `big_shift_horizontal_{heatmap,signal}.png`,
  `big_shift_horizontal_signal_data.{csv,tex}` (+ vertical counterparts).
  `README.md` documents the 16px-periodicity FFT methodology precisely: small
  shift ±32px step 2 (32 points, resolves periods at 64/k), big shift ±384px
  step 64; periodicity peak expected/observed near 16px, matching transformer
  patchification grid size.
- `CONSISTENCY/`: `MASTER_CONSISTENCY.xlsx` plus the standard
  `metrics_no_punc/` set — likely the unchanged-repeat control family.

## 3. `repository chat2rec_analysis/projects/pa_death_records622_official`

Largest root inspected (13,835 files at depth 3; dominated by 13,727 `.json`
files — raw/derived per-response records). Top-level dirs (14, depth 1):
`analysis`, `analysis - v1` through `analysis - v7` (with a `v6a` in between),
`backup`, `gemini`, `gemini - confidence_analysis`, `metrics_no_punc`, `paper`.

- Each `analysis*/` snapshot has `best_consensus_CER/cv_rank_metrics_summary.tsv`
  and `paper/{ensemble_methods_comparison.png,experiment_type_chart_0.png,
  top_cer_table.tex,top_rank_table.tex}`. `v6` adds `_no_title` chart variants.
  `v7` uniquely adds `ensemble_methods_table.tex` and
  `transform_metrics_table.tex` — likely the most complete/final analysis pass
  and the best candidate for cross-checking the `GOALS_AUDIT.md` shortlist.
  Multiple numbered snapshots (v1-v7) suggest iterative paper-era analysis
  revisions; `v2` is explicitly labeled "seam carving," indicating the
  snapshots are not simple reruns but different analysis variants/methods,
  not strictly a linear version history.
- `gemini/`: 12,499 files; raw per-image JSON responses, at least one
  subdirectory named `gemini-20-flash_blur_11/` visible at this depth,
  confirming a per-experiment-condition directory layout consistent with
  `PA_DEATH`'s structure. `failed_transcriptions.docx` (+ Office lock file
  `~$iled_transcriptions.docx`, indicating the docx was open/edited at some
  point) also present at this root — likely a manual QA note, not a data file.
- `gemini - confidence_analysis/`: 1,250 files, same per-image JSON layout
  pattern (sampled filenames match the `SHIFT` root's response naming), a
  parallel confidence-scored response tree.
- `5164_gts.pkl` / `5164_gts_no_post_processing.pkl` also present at this
  root's top level — consistent with the older public/v7 processed lineage,
  but **not** evidence that the same processed file is used by the newer
  paper/v9/v10 root. See [`docs/GT_LINEAGE.md`](../docs/GT_LINEAGE.md).

## 4. `repository ancestry/chat2rec_v1/chat2rec/degradations`

Transform implementation source, 42 files, 6 dirs.
- Top level: `config.py`, `generate_variants.py`, `pipeline.py`,
  `test_config_example.py`, `__init__.py`.
- `apps/`: interactive GUIs for manual parameter tuning —
  `handwriting_gridwarp_gui.py`, `simple_gridwarp_gui.py`,
  `handwriting_convolution_gui.py`, `handwriting_kernel_gui.py`,
  `handwriting_cloud_gui.py`.
- `effects/`: `gridwarp.py`, `gridwarp2.py`,
  `handwriting_aware_gridwarp{,_v1,_o3_incompat,_v_fast_but_bad}.py`,
  `handwriting_cloud_warp.py`, `handwriting_convolution.py`,
  `handwriting_kernel_warp.py`, `shift.py`, `blur.py`, `resize.py`, `noise.py`,
  `composite.py`, `seamcarve.py`, `seamcarve2.py`, `seam_removal.py`,
  `segmentation_transforms.py`, `albumentations_adapter.py`, `base.py`.
  Multiple named variants of grid-warp (`gridwarp` vs `gridwarp2` vs several
  `handwriting_aware_gridwarp*`) mean the exact implementation used for the
  historical `WARP/PA_DEATH_WARP.yaml` runs is not yet confirmed by filename
  alone — needs a direct read of `pipeline.py`/`config.py` wiring, not
  performed in this bounded pass.
- `effects/dev/seamcarve_standalone.py`; `seamcarving/seamcarver.py`.
- `semantic_segmentation/`: `cropper.py`, `cropper2.py`, `save_as_csv.py`,
  `visualizer_v1..v4.py` — likely feeds the `mask_root` referenced in
  `PA_DEATH_WARP.yaml` (`external semantic-segmentation mask root (machine path retained only in the ignored local manifest)`).

## 5. `repository ancestry/vermont`

Modern-model execution repo, 215 files at depth 3.
- Top level: `Makefile`, `README.md`, `S3_MEDIA_ACCESS.md`,
  `create_vermont_contact_sheets.ps1`, `pyproject.toml`, `uv.lock`,
  `vermont_occlusion_package.py`.
- `deploy/`: `dispatcher/` (`app.py`, `polling.py`, `sagemaker_resolve.py`,
  `consolidation.py`, `notifications.py`), `worker/` (`worker.py`, `sqs.py`,
  `s3.py`), `infra/` (Terraform: `ecs.tf`, `sqs.tf`, `dynamodb.tf`, `alb.tf`,
  `autoscaling.tf`, `s3.tf`, `s3_redirector.tf`, `iam.tf`, `example.tfvars`),
  `s3_redirector/lambda_handler.py`, `local/run_local.sh`,
  `credentials/gcp-service-account.json.example` (example/template only).
- `models/example/`: `model.py`, `input.json` — example model contract.
- `63129.IDX.003_field_extraction/` (one level deeper than the depth-3 scan
  cutoff, located via targeted grep): `scripts/qwen_warmup_and_smoke.py`
  (confirms `MODEL = "sagemaker-qwen3-vl-8b-instruct-fp8"` at line 17),
  `run_qwen_cells.py`, `smoke_qwen.py`, `smoke_qwen_dev.py`,
  `smoke_qwen_off.py`, `run_iteration.py`, `build_report{,_v2..v5}.py`,
  `find_capture_naturalization_examples.py`; docs `AGENTS.md`,
  `BATCH_PIPELINE.md`, `RESULTS_SUMMARY.md`, `OPERATOR_REPORT.md`, and a
  `BROWN_BAG_2026_08_19/` deck-prep folder with numbered planning docs
  (`01_EXPERIMENT_PLAN.md` … `13_EVIDENCE_AND_SPEAKER_NOTES_HANDBOOK.md`).
  `.tickets/` has 20 numbered tickets, including
  `TICKET-005-flashlite-high-parse-failure.md` (directly relevant to the
  Flash-Lite screen) and `TICKET-006-selective-review-pixel-offset-char-decomp.md`
  (directly relevant to offset/confidence review UX).
- `FILE_INDEX.tsv`: a repo-generated manifest with columns
  `relative_path, bytes, extension, kind, tracked, reason` — useful precedent
  for this repo's own manifest format.

## 6. `repository ancestry/ds-content-raptor`

Framework/tickets repo, 321 files at depth 3.
- Top level: `.gitignore`, `FILE_INDEX.tsv`, `README.md`,
  `REPOSITORY_SCOPE.md`, `TAYLOR_READ_ME.md`.
- `.kiro/steering/batch-workflow.md`.
- `.tickets/`: `AGENT_EXPERIMENT_LOOP.md`, `GOAL.md`, and
  `TICKET-001` through `TICKET-020` (same numbering scheme as `vermont`'s
  tickets, but distinct titles — e.g. `TICKET-007-bbox-localization-baseline.md`,
  `TICKET-018-show-all-tta-experiments.md`).
- `63129.IDX.003_field_extraction/`: same subtree name as in `vermont`
  (`AGENTS.md`, `BATCH_PIPELINE.md`, `README.md`, `OPERATOR_REPORT.md`,
  `RESULTS_SUMMARY.md`, `.tickets/STATUS.md`, `ANALYSIS/README.md`,
  `BROWN_BAG_2026_08_19/` with the same 13 numbered docs). **Flagged in
  `GOALS_AUDIT.md` as an unresolved discrepancy** — needs a direct diff
  against the `vermont` copy, not assumed identical or assumed stale.
- Extension profile at depth 3: 144 `.py`, 126 `.md`, 40 `.txt`, 3 `.ps1`,
  3 `.sh` — code- and doc-heavy, consistent with "framework," not raw data.

## Cross-root identity checks performed

- Confirmed identical ground-truth pickle basenames (`5164_gts.pkl`,
  `5164_gts_no_post_processing.pkl`) recur in `PA_DEATH/{BIG_SHIFT,CONSISTENCY,
  SHIFT,WARP}` and at the top of `chat2rec_analysis/.../gemini/` — same
  canonical label set referenced from multiple analysis trees.
- Confirmed shared image basenames between `pa-death-records-622/examples/samples/`
  and the `chat2rec_analysis` gemini response filenames
  (`41381_1220705043_0549-04785`, `41381_1220705043_0567-00432`) — same 622
  cohort.
- Confirmed `models/gemini-2.0-flash` and `temperature: 0` consistently in both
  `PA_DEATH/WARP/PA_DEATH_WARP.yaml` and `run_settings.csv` header/first data
  row sampled from `PA_DEATH/SHIFT/metrics_no_punc/`.

## Explicitly not done in this pass (scope discipline)

- Did not open/parse any `.pkl` files (would require a Python session and
  wasn't necessary to establish provenance/paths).
- Did not fully parse `run_settings.csv` or `ensemble_selection_analysis.tsv`
  beyond a 2-3 row preview — full ranking extraction is flagged as follow-up
  in `GOALS_AUDIT.md`. **SUPERSEDED/RESOLVED**: the addenda at the top of
  this file and of `GOALS_AUDIT.md` record the full extraction (WARP config
  SHA-256, 14 active/18 commented variants, exact v4 ranking, `SHIFT`
  `run_settings.csv` three-row parse).
- Did not read `chat2rec_v1/.../pipeline.py` or `config.py` to confirm which
  `gridwarp*.py` implementation the historical `WARP` runs actually invoked.
  **SUPERSEDED/RESOLVED**: the addendum confirms active WARP rows use
  registry type `handwriting_kernel_warp`, not `gridwarp.py`/`gridwarp2.py`.
- Did not diff the two `63129.IDX.003_field_extraction` subtrees
  (`vermont` vs `ds-content-raptor`). Still open/unresolved.
- Did not search for exact modern Gemini Flash 3.7 / Flash-Lite 3.5 model IDs
  beyond the six bounded roots' depth-3/grep results already gathered; none
  were found in what was inspected. **SUPERSEDED/RESOLVED**: the addendum
  records the exact resolved IDs (`gemini-3.7-flash`, `gemini-3.5-flash-lite`)
  found via targeted follow-up search outside the original six-root scan.
